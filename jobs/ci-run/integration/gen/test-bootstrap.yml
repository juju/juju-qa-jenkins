

# Code generated by gen-wire-tests. DO NOT EDIT.
- job:
    name: 'test-bootstrap-multijob'
    project-type: 'multijob'
    description: |-
        Test bootstrap Suite
    condition: SUCCESSFUL
    node: noop-parent-jobs
    concurrent: true
    wrappers:
    - ansicolor
    - workspace-cleanup
    - timestamps
    parameters:
    - string:
        default: ''
        description: 'Enable sub job to be run individually.'
        name: SHORT_GIT_COMMIT
    - string:
        default: ''
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
    builders:
    - get-s3-build-details
    - set-test-description
    - multijob:
        name: 'IntegrationTests-bootstrap'
        projects:
        - name: 'test-bootstrap-test-bootstrap-controller-snap-path-lxd'
          current-parameters: true
        - name: 'test-bootstrap-test-bootstrap-controller-snap-path-without-assert-lxd'
          current-parameters: true
        - name: 'test-bootstrap-test-bootstrap-simplestream-lxd'
          current-parameters: true

- job:
    name: test-bootstrap-test-bootstrap-controller-snap-path-lxd
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_bootstrap_controller_snap_path in bootstrap suite on lxd
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'localhost'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'lxd'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[5-9].*|^4\\.([1-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test:
                  test_name: 'bootstrap'
                  setup_steps: ''
                  task_name: 'test_bootstrap_controller_snap_path'
                  skip_tasks: 'test_bootstrap_controller_snap_path_without_assert,test_bootstrap_simplestream'
    publishers:
      - integration-artifacts

- job:
    name: test-bootstrap-test-bootstrap-controller-snap-path-without-assert-lxd
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_bootstrap_controller_snap_path_without_assert in bootstrap suite on lxd
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'localhost'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'lxd'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[5-9].*|^4\\.([1-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test:
                  test_name: 'bootstrap'
                  setup_steps: ''
                  task_name: 'test_bootstrap_controller_snap_path_without_assert'
                  skip_tasks: 'test_bootstrap_controller_snap_path,test_bootstrap_simplestream'
    publishers:
      - integration-artifacts

- job:
    name: test-bootstrap-test-bootstrap-simplestream-lxd
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_bootstrap_simplestream in bootstrap suite on lxd
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'localhost'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'lxd'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - run-integration-test:
            test_name: 'bootstrap'
            setup_steps: ''
            task_name: 'test_bootstrap_simplestream'
            skip_tasks: 'test_bootstrap_controller_snap_path,test_bootstrap_controller_snap_path_without_assert'
    publishers:
      - integration-artifacts
