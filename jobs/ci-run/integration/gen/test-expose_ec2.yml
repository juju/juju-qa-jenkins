

# Code generated by gen-wire-tests. DO NOT EDIT.
- job:
    name: 'test-expose_ec2-multijob'
    project-type: 'multijob'
    description: |-
        Test expose_ec2 Suite
    condition: SUCCESSFUL
    node: noop-parent-jobs
    concurrent: true
    wrappers:
    - ansicolor
    - workspace-cleanup
    - timestamps
    parameters:
    - string:
        default: ''
        description: 'Enable sub job to be run individually.'
        name: SHORT_GIT_COMMIT
    - string:
        default: ''
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
    - string:
        default: ''
        description: 'Ubuntu series to use when bootstrapping Juju'
        name: BOOTSTRAP_SERIES
    builders:
    - get-build-details
    - set-test-description
    - multijob:
        name: 'IntegrationTests-expose_ec2'
        projects:
        - name: 'test-expose_ec2-test-bundle-with-exposed-endpoints-aws'
          current-parameters: true
        - name: 'test-expose_ec2-test-expose-app-ec2-aws'
          current-parameters: true

- job:
    name: test-expose_ec2-test-bundle-with-exposed-endpoints-aws
    node: ephemeral-focal-small-amd64
    description: |-
      Test test_bundle_with_exposed_endpoints in expose_ec2 suite on aws
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{7}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'aws'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'aws'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    - string:
        default: 'us-east-1'
        description: 'Cloud Region to use when bootstrapping Juju'
        name: BOOTSTRAP_REGION
    - string:
        default: ''
        description: 'Ubuntu series to use when bootstrapping Juju'
        name: BOOTSTRAP_SERIES
    - string:
        default: jujuqabot
        description: "Operator docker image account name."
        name: OPERATOR_IMAGE_ACCOUNT
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 50
          fail: true
          type: absolute
    builders:
      - run-integration-test:
            test_name: 'expose_ec2'
            setup_steps: ''
            task_name: 'test_bundle_with_exposed_endpoints'
            skip_tasks: 'test_expose_app_ec2'

    publishers:
      - integration-artifacts

- job:
    name: test-expose_ec2-test-expose-app-ec2-aws
    node: ephemeral-focal-small-amd64
    description: |-
      Test test_expose_app_ec2 in expose_ec2 suite on aws
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{7}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'aws'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'aws'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    - string:
        default: 'us-east-1'
        description: 'Cloud Region to use when bootstrapping Juju'
        name: BOOTSTRAP_REGION
    - string:
        default: ''
        description: 'Ubuntu series to use when bootstrapping Juju'
        name: BOOTSTRAP_SERIES
    - string:
        default: jujuqabot
        description: "Operator docker image account name."
        name: OPERATOR_IMAGE_ACCOUNT
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 50
          fail: true
          type: absolute
    builders:
      - run-integration-test:
            test_name: 'expose_ec2'
            setup_steps: ''
            task_name: 'test_expose_app_ec2'
            skip_tasks: 'test_bundle_with_exposed_endpoints'

    publishers:
      - integration-artifacts
