

# Code generated by gen-wire-tests. DO NOT EDIT.
- job:
    name: 'test-storage_k8s-multijob'
    project-type: 'multijob'
    description: |-
        Test storage_k8s Suite
    condition: SUCCESSFUL
    node: noop-parent-jobs
    concurrent: true
    wrappers:
    - ansicolor
    - workspace-cleanup
    - timestamps
    parameters:
    - string:
        default: ''
        description: 'Enable sub job to be run individually.'
        name: SHORT_GIT_COMMIT
    - string:
        default: ''
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
    builders:
    - get-s3-build-details
    - set-test-description
    - multijob:
        name: 'IntegrationTests-storage_k8s'
        projects:
        - name: 'test-storage_k8s-test-add-unit-attach-storage-microk8s'
          current-parameters: true
        - name: 'test-storage_k8s-test-add-unit-attach-storage-scaling-race-condition-microk8s'
          current-parameters: true
        - name: 'test-storage_k8s-test-add-unit-duplicate-pvc-exists-microk8s'
          current-parameters: true
        - name: 'test-storage_k8s-test-deploy-attach-storage-microk8s'
          current-parameters: true
        - name: 'test-storage_k8s-test-force-import-filesystem-microk8s'
          current-parameters: true
        - name: 'test-storage_k8s-test-import-filesystem-microk8s'
          current-parameters: true

- job:
    name: test-storage_k8s-test-add-unit-attach-storage-microk8s
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_add_unit_attach_storage in storage_k8s suite on microk8s
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'microk8s'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'k8s'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[4-9].*|^3\\.([6-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test-microk8s:
                  test_name: 'storage_k8s'
                  setup_steps: ''
                  task_name: 'test_add_unit_attach_storage'
                  skip_tasks: 'test_add_unit_attach_storage_scaling_race_condition,test_add_unit_duplicate_pvc_exists,test_deploy_attach_storage,test_force_import_filesystem,test_import_filesystem'
    publishers:
      - integration-artifacts

- job:
    name: test-storage_k8s-test-add-unit-attach-storage-scaling-race-condition-microk8s
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_add_unit_attach_storage_scaling_race_condition in storage_k8s suite on microk8s
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'microk8s'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'k8s'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[4-9].*|^3\\.([6-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test-microk8s:
                  test_name: 'storage_k8s'
                  setup_steps: ''
                  task_name: 'test_add_unit_attach_storage_scaling_race_condition'
                  skip_tasks: 'test_add_unit_attach_storage,test_add_unit_duplicate_pvc_exists,test_deploy_attach_storage,test_force_import_filesystem,test_import_filesystem'
    publishers:
      - integration-artifacts

- job:
    name: test-storage_k8s-test-add-unit-duplicate-pvc-exists-microk8s
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_add_unit_duplicate_pvc_exists in storage_k8s suite on microk8s
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'microk8s'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'k8s'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[4-9].*|^3\\.([6-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test-microk8s:
                  test_name: 'storage_k8s'
                  setup_steps: ''
                  task_name: 'test_add_unit_duplicate_pvc_exists'
                  skip_tasks: 'test_add_unit_attach_storage,test_add_unit_attach_storage_scaling_race_condition,test_deploy_attach_storage,test_force_import_filesystem,test_import_filesystem'
    publishers:
      - integration-artifacts

- job:
    name: test-storage_k8s-test-deploy-attach-storage-microk8s
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_deploy_attach_storage in storage_k8s suite on microk8s
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'microk8s'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'k8s'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[4-9].*|^3\\.([6-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test-microk8s:
                  test_name: 'storage_k8s'
                  setup_steps: ''
                  task_name: 'test_deploy_attach_storage'
                  skip_tasks: 'test_add_unit_attach_storage,test_add_unit_attach_storage_scaling_race_condition,test_add_unit_duplicate_pvc_exists,test_force_import_filesystem,test_import_filesystem'
    publishers:
      - integration-artifacts

- job:
    name: test-storage_k8s-test-force-import-filesystem-microk8s
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_force_import_filesystem in storage_k8s suite on microk8s
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'microk8s'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'k8s'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[4-9].*|^3\\.([6-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test-microk8s:
                  test_name: 'storage_k8s'
                  setup_steps: ''
                  task_name: 'test_force_import_filesystem'
                  skip_tasks: 'test_add_unit_attach_storage,test_add_unit_attach_storage_scaling_race_condition,test_add_unit_duplicate_pvc_exists,test_deploy_attach_storage,test_import_filesystem'
    publishers:
      - integration-artifacts

- job:
    name: test-storage_k8s-test-import-filesystem-microk8s
    node: ephemeral-noble-8c-32g-amd64
    concurrent: true
    description: |-
      Test test_import_filesystem in storage_k8s suite on microk8s
    parameters:
    - validating-string:
        name: SHORT_GIT_COMMIT
        description: 'Enable sub job to be run individually.'
        regex: ^\S{{7}}$
        msg: Enter a valid 7 char git sha
    - choice:
        default: 'amd64'
        description: 'Build arch used to download the build tar.gz.'
        name: BUILD_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used to boostrap controller.'
        name: BOOTSTRAP_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - choice:
        default: ''
        description: 'Arch used for hosted models.'
        name: MODEL_ARCH
        choices:
        - amd64
        - arm64
        - s390x
        - ppc64el
    - string:
        default: 'microk8s'
        description: 'Cloud to use when bootstrapping Juju'
        name: BOOTSTRAP_CLOUD
    - string:
        default: 'k8s'
        description: 'Provider to use when bootstrapping Juju'
        name: BOOTSTRAP_PROVIDER
    wrappers:
      - default-integration-test-wrapper
      - timeout:
          timeout: 30
          fail: true
          type: absolute
    builders:
      - common
      - select-oci-registry
      - prepare-integration-test
      - conditional-step:
          # Only run on regexp version match.
          # Accounts for tests which do not exist
          # until a given Juju version.
          condition-kind: regex-match
          regex: "^[4-9].*|^3\\.([6-9]|\\d{{2,}})(\\.|-).*"
          label: "${{JUJU_VERSION}}"
          on-evaluation-failure: "dont-run"
          steps:
            - run-integration-test-microk8s:
                  test_name: 'storage_k8s'
                  setup_steps: ''
                  task_name: 'test_import_filesystem'
                  skip_tasks: 'test_add_unit_attach_storage,test_add_unit_attach_storage_scaling_race_condition,test_add_unit_duplicate_pvc_exists,test_deploy_attach_storage,test_force_import_filesystem'
    publishers:
      - integration-artifacts
